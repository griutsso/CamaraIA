# ═══════════════════════════════════════════════════════════════
#  IA-CAM-SERVICE — Dependencias Python
# ═══════════════════════════════════════════════════════════════

# ── Core ──
numpy>=1.24.0,<2.0
opencv-python>=4.8.0
Pillow>=10.0.0
PyYAML>=6.0

# ── UI Web (modo por defecto) ──
flask>=3.0.0

# ── UI Desktop (modo --mode gui) ──
customtkinter>=5.2.0

# ── Inteligencia Artificial ──
mediapipe>=0.10.0        # Detección de rostros (fallback)
ultralytics>=8.0.0       # YOLO (detección de rostros, placas, objetos)
easyocr>=1.7.0           # OCR para placas vehiculares

# ── Monitoreo (reservado para implementación futura) ──
# psutil>=5.9.0          # Métricas de sistema (RAM, CPU, temp) — sin uso actual

# ── Seguridad (reservado para Fase 4) ──
# cryptography>=41.0.0   # Cifrado AES-256-GCM — pendiente de implementar
